#Contraste

for i in range(img.shape[0]) :
    for j in range(img.shape[1]) :
        for k in range(3) :
            if img[i,j,k]>150 :
                img[i,j,k]=255
            else :
                img[i,j,k]=0
